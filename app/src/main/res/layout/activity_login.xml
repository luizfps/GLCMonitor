<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:text="Nome de usuário"
        android:ems="10"
        android:id="@+id/textNomeDeUsuario"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="139dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:ems="10"
        android:id="@+id/textSenha"
        android:allowUndo="false"
        android:layout_below="@+id/textNomeDeUsuario"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        android:id="@+id/buttonLogin"
        android:layout_below="@+id/textSenha"
        android:layout_alignRight="@+id/textSenha"
        android:layout_alignEnd="@+id/textSenha"
        android:layout_alignLeft="@+id/textSenha"
        android:layout_alignStart="@+id/textSenha"
        android:onClick="onCallLogar" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cadastre-se"
        android:id="@+id/buttonCadastro"
        android:layout_below="@+id/buttonLogin"
        android:layout_alignLeft="@+id/textSenha"
        android:layout_alignStart="@+id/textSenha"
        android:layout_marginTop="49dp"
        android:layout_alignRight="@+id/buttonLogin"
        android:layout_alignEnd="@+id/buttonLogin"
        android:onClick="onCallCadastro" />

</RelativeLayout>
